events {}

http {
    upstream frontend {
        server frontend:80;
    }
    upstream rest_api {
        server rest-api:80;
    }
    upstream ws_api {
        server ws-api:80;
    }

    server {
        listen 80 default_server;
        return 444; #TODO: Change to actual routing error handling later
    }

    server {
        listen 80;
        server_name blazz1t.online api.blazz1t.online ws.blazz1t.online;
        return 301 https://$host$request_uri;
    }

    server {
        listen 443 ssl;
        server_name blazz1t.online;

        ssl_certificate /etc/ssl/cloudflare.crt;
        ssl_certificate_key /etc/ssl/private/cloudflare.key;

        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_prefer_server_ciphers on;
        ssl_ciphers HIGH:!aNULL:!MD5;

        location / {
            proxy_pass http://frontend/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }

    server {
        listen 443 ssl;
        server_name api.blazz1t.online;

        ssl_certificate /etc/ssl/cloudflare.crt;
        ssl_certificate_key /etc/ssl/private/cloudflare.key;

        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_prefer_server_ciphers on;
        ssl_ciphers HIGH:!aNULL:!MD5;

        location / {
            proxy_pass http://rest_api/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }

    server {
        listen 443 ssl;
        server_name ws.blazz1t.online;

        ssl_certificate /etc/ssl/cloudflare.crt;
        ssl_certificate_key /etc/ssl/private/cloudflare.key;

        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_prefer_server_ciphers on;
        ssl_ciphers HIGH:!aNULL:!MD5;

        location / {
            proxy_pass http://ws_api/;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}
